{
  "//": "LubbDubb example config. Copy to lubbdubb.config.json (gitignored). Every key is OPTIONAL — omit any to take its default. The '//'-prefixed keys are inline docs; they are ignored at load, so this file stays copy-and-run. As shipped it drives the mock agent (agentMode 'raw' + fake integrations), so `npm start` works with no model or provider auth. Precedence: explicit overrides > env (PORT, LUBBDUBB_HOST, LUBBDUBB_DB, LUBBDUBB_REPO_ROOT) > this file > defaults. Six keys are DEEP-MERGED rather than replaced, so you can set one field of them alone: integrations, planning, spendBurn, selfUpdate, validation, auth. Everything else — including issuePriorityLabels and ci.checks — is replaced wholesale. Since #401 the cockpit's Settings tab edits this file directly — it writes the keys you change and leaves these comments, the key order and everything else alone, so hand-editing and the form are two ways at one file. The file is laid out in the SAME SIX SECTIONS the Settings tab draws (Dispatch, Agents, Integrations, Features, Paths, Server), so a key is in the same place in both. Full reference: docs/spec/02-configuration.md.",

  "// ===== DISPATCH =====": "The pulse, and how much of the fleet it may spend.",

  "// heartbeatIntervalMs": "How often (ms) the heartbeat fires a dispatch cycle. Default 300000 (5 min).",
  "heartbeatIntervalMs": 300000,

  "// maxConcurrentAgents": "Hard cap on concurrently-running agents; seeds the live runtime control. Adjustable without restart via POST /api/control; a restart reverts to this value. Default 3.",
  "maxConcurrentAgents": 3,

  "// startPaused": "Boot with dispatch paused (no new agents until resumed). The only config-level pause knob — live pause/resume is runtime-only and ephemeral. Default false.",
  "startPaused": false,

  "// closedPrWindowMs": "How far back a provider looks for pull requests that have LEFT the open set, so a merged or abandoned PR is observed rather than inferred from its disappearance. Feeds the cockpit's 'Recently closed' list, the pr_merged/pr_closed world events, and plan reconciliation's ability to tell a merge from an abandonment. Costs one extra list request per snapshot per provider (no per-PR fan-out). Default 21600000 (6h); 0 disables the lookup, and every consumer falls back to the older 'absence means merged' reading.",
  "closedPrWindowMs": 21600000,

  "// upNextOverrideTtlMs": "How long an operator 'Up next' priority override survives after the harness stops tracking its origin. The override is refreshed every pulse its origin is still queued or staffed, so a long-running item keeps its priority; once the work is gone (merged, closed, abandoned) for this long the stale override is pruned. Default 604800000 (7d); 0 disables pruning entirely.",
  "upNextOverrideTtlMs": 604800000,

  "// ===== AGENTS =====": "How one is launched, what it may run, what it runs on, and how it is nudged.",

  "// agentMode": "How agents are launched. 'stream' = real Claude Code over headless stream-JSON (production default, runs unattended, supports the waiting/answer loop). 'pty' = real Claude Code as an interactive terminal (needs first-run onboarding done; this is the runtime that resumes across restarts). 'raw' = run claudeCommand/claudeArgs verbatim, prompt passed via LUBBDUBB_PROMPT (mock-agent demo + tests). Default 'stream'.",
  "agentMode": "raw",

  "// claudeCommand": "The agent binary to launch. Default 'claude'. Here it's 'bash' so the example can run the mock agent.",
  "claudeCommand": "bash",

  "// claudeArgs": "Extra args appended AFTER the harness's own, so an explicit flag here has the last word — which is why you must NOT pass --allowedTools here (it would silently drop the mcp__lubbdubb__* grants; use agentAllowedTools instead). A relative path naming an existing file is resolved to absolute at load (agents run in a worktree cwd). Default [].",
  "claudeArgs": ["scripts/mock-agent.sh"],

  "// agentPermissionMode": "Passed to `claude --permission-mode` so unattended tool calls don't hang. Default 'acceptEdits', which auto-accepts FILE EDITS ONLY — shell commands are covered by agentAllowedTools + the permission backstop below, so you should rarely need 'bypassPermissions' (which maps to --dangerously-skip-permissions and is refused by claude under root).",
  "agentPermissionMode": "acceptEdits",

  "// agentAllowedTools": "Tool allow rules handed to every agent as a `permissions.allow` fragment in --settings, in Claude Code rule syntax. These pre-approve the mechanical validate/commit/push commands an agent must run to take an issue through to an opened PR unattended. Anything NOT listed still hits the permission prompt, which the backstop (mcp.permissionEscalation) routes to you rather than hanging. Replaced wholesale. Default: the JS toolchain + git + gh, as shown.",
  "agentAllowedTools": [
    "Bash(npm:*)",
    "Bash(npx:*)",
    "Bash(pnpm:*)",
    "Bash(yarn:*)",
    "Bash(node:*)",
    "Bash(git:*)",
    "Bash(gh:*)"
  ],

  "// agentModels": "Which model each KIND of work runs on, and how hard — keyed on the dispatch rule that proposed it. A rule points at a NAMED PROFILE, and a profile is a model plus an optional effort level and nothing else (claudeArgs stays the single global escape hatch, so a profile can never clobber the --allowedTools MCP grants). The name is the stable part: when a new model ships, one profile value changes and every rule pointing at it follows. Resolved ONCE at dispatch and stored on the task, so an agent resumed after a restart re-launches on what it started on and the run stays auditable — the cockpit's agent drawer shows it, and 'by task type' spend prices it. NEITHER value is validated here: only the installed claude knows which models exist and which of them take --effort, so a bad one fails at SPAWN as a failed agent, not at boot. What IS refused at boot: a profile written as a bare string, an unknown effort level, a byRule key that is not a pipeline rule id, and any name that resolves to no profile. The whole block is OPTIONAL and merges WHOLE rather than field by field — setting it replaces it, which is what lets you REMOVE an assignment rather than only add one. Omitted entirely, no launch carries either flag and the CLI's own defaults apply — which is NOT a neutral setting: the CLI defaults to the top of the effort ladder, so an unconfigured fleet is the expensive one. The values below are a worked shape, not a recommendation: substitute the models your CLI actually has.",
  "agentModels": {
    "// profiles": "Profile name to what it launches. 'model' is anything `claude --model` accepts: an alias (as below) tracks whatever is current for that tier, a full model id pins one exactly — pin when you want a stored run to say precisely what it cost on, since Task.model records the string as written. 'effort' is optional; omitted, the launch carries no --effort at all. The 'fast' profile below deliberately sets none, because the smallest models refuse the flag outright — so cheap-and-shallow is one lever OR the other, never both. 'rank' and 'description' are BOTH REQUIRED and are read by the harness rather than passed to the CLI. rank orders the profiles cheapest-first and must be unique: it is how the goal-profile gate says whether a proposal is cheaper or deeper than what is standing, and how the cockpit orders its dropdowns. description is the whole of what the ASSAYER is told about your profiles when it proposes one for an issue — write it as instructions to an agent about when to pick this profile, not as a note to yourself.",
    "profiles": {
      "fast": {
        "model": "haiku",
        "rank": 1,
        "description": "Mechanical, well-specified work with an obvious shape: a one-line fix, a rename, a config change, a test that just needs writing."
      },
      "standard": {
        "model": "sonnet",
        "effort": "medium",
        "rank": 2,
        "description": "Ordinary feature and bug work — the change is contained, the approach is clear from the ticket, and no design decision is being made."
      },
      "deep": {
        "model": "opus",
        "effort": "medium",
        "rank": 3,
        "description": "Work whose shape is genuinely unclear, that spans several subsystems, that has to weigh a design trade-off, or where getting the approach wrong is expensive to undo."
      }
    },
    "// default": "The profile every rule with no byRule entry runs on, AND every dispatch composed outside a rule. Set it: leaving it unset is not 'the middle', it is every unassigned rule falling through to the CLI's own defaults, which is the top of the ladder. One line here moves the whole fleet.",
    "default": "deep",
    "// byRule": "The exceptions, by dispatch rule id. Only the rules that are genuinely cheaper than the default need an entry. Two things worth knowing before you extend this list: several rules SPEND NO AGENT AT ALL — work-item-in-review and work-item-back-to-pickup write a tracker state, pr-merge-ready merges, pr-ci-blocked/plan-approval/plan-blocked/issue-shortfall only ask a human, and pr-base-update's cheap arm is a provider-side merge — so assigning them a profile reads as a saving and buys nothing. And a rule whose verdict GATES something (issue-assay stops the funnel, issue-assess stops pickup) is a poor candidate for a smaller model even though its output is short; give it less effort instead.",
    "byRule": {
      "pr-ci-gate": "fast",
      "issue-retro": "fast",
      "issue-assess": "standard",
      "validate-check": "standard"
    },
    "// PINS": "byRule prices work by KIND, which has no answer for the one issue that is harder than the rule it arrived on. That is a TAG on the ticket, not a config key: '<labelPrefix>-model-<profile>' pins one goal's work to one profile, and it beats byRule for every dispatch on that issue except issue-retro (fixed to its byRule entry — a write-up gates nothing, so a deep pin there is money for a document no dispatch reads) and issue-assay (the stage that PROPOSES the pin, so it cannot run on it). Nothing needs configuring to turn this on; it is on wherever labelPrefix and profiles are both set. The assayer proposes a profile for every workable goal, and a proposal that differs from what is already standing HOLDS the funnel until you confirm it from the cockpit — one click, and an absent proposal holds nothing, so a crashed or capped assayer can never park an issue. A plan may also name a profile per part, which beats the goal's pin for that part alone. See docs/spec/02-configuration.md."
  },

  "// agentPromptDelayMs": "Wait this long (ms) after spawn before delivering the task, giving an interactive REPL time to boot. Stream mode uses 0. Default 1200.",
  "agentPromptDelayMs": 1200,

  "// agentSubmitDelayMs": "PTY only. Gap (ms) between typing a message and sending its submitting carriage return — the claude TUI folds a burst into a paste and treats a glued-on CR as a literal newline, leaving the text unsubmitted. Set 0 to write both at once. Default 60.",
  "agentSubmitDelayMs": 60,

  "// agentIdleWaitMs": "PTY only. Safety net for a turn that ends without a sentinel: after this long (ms) with no terminal output at all, the session is parked as waiting so it reaches the inbox instead of sitting 'running' forever. Unlatched — output resuming un-parks it. 0 disables. Default 90000.",
  "agentIdleWaitMs": 90000,

  "// agentWaitingPatterns": "Extra literal substrings that mean 'the CLI is waiting for input' (backup escalation heuristic, PTY). Default [].",
  "agentWaitingPatterns": [],

  "// agentStallNudges": "How many times an agent that ends a turn with no sentinel in it is asked to account for itself before the stop is put to a human. Most such stops are a forgotten done sentinel, or an agent waiting on a build or CI that nothing would have woken it from — neither has anything for a person to answer. A whole-life budget per agent; 0 parks on the first one. Stream runtime only. Default 2.",
  "agentStallNudges": 2,

  "// agentResumeAttempts": "How many times an agent whose process dies mid-run is re-attached to its own conversation before the harness settles it as failed. Counted on the agent row, so it survives a restart and bounds the agent's whole life, not one launch. 0 disables. Default 3.",
  "agentResumeAttempts": 3,

  "// lessonBlockChars": "How many characters of PROMOTED lessons ride in every agent's system prompt (#355). Characters rather than a count, because what is being bounded is context and a lesson runs from one line to 2,000 characters. The block is identical for every agent in the fleet, so it is a cached prefix paid once rather than per dispatch — which is also why nothing per-dispatch may go in it. Over the cap, whole lessons are dropped OLDEST-VOUCHED FIRST (never a truncated claim, which would be a claim nobody promoted), and the drop is shown to you per row in the cockpit's Lessons panel — the agent is never told the list it reads is partial. 0 renders nothing at all, and then launch arguments are byte-identical to a build without the feature. Default 6000.",
  "lessonBlockChars": 6000,

  "// whitelistedApprovals": "PTY waiting prompts the harness may auto-answer instead of escalating. Each rule: { match: <substring of the waiting prompt>, response: <text typed back> }. Default [].",
  "whitelistedApprovals": [{ "match": "Allow running tests", "response": "yes" }],

  "// sessionTranscriptRoot": "Where Claude Code keeps per-project session transcripts, which PTY mode tails for its legible transcript. Default '~/.claude/projects'. Override only if the agent runs with a different HOME than the server.",
  "sessionTranscriptRoot": "~/.claude/projects",

  "// ===== INTEGRATIONS =====": "Who you are, which providers fulfil which capability, and which items count as work.",

  "// userId": "WHO YOU ARE, to every provider the harness talks to — the one identity it acts on behalf of. Setting it turns on three gates together, because they are one intent (this harness works MY queue): (1) OWNERSHIP — '<labelPrefix>-watch' only counts if YOU added it, so nobody else can tag work onto the fleet; (2) ASSIGNMENT — tickets the harness FILES are assigned to you; (3) AUTHORSHIP — only pull requests you opened are surfaced, which is also what lets a merged branch of yours be reaped. One string rather than one per provider because one project is worked at a time and each project carries its own config file: it is a GitHub login where integrations selects 'github', an Azure UPN where it selects 'azure', and only one of those is ever in force. UNSET (the default) leaves all three gates off — any tagger counts, filed tickets go unassigned, every open PR is surfaced. That is the first-run posture, and it is why the 'fake' provider works with no identity at all.",
  "userId": "you@acme.com",

  "// integrations": "Which provider fulfils each capability — the swap switch. One provider per capability, deep-merged so you can override just one. Providers: 'fake' (editable, injectable world — the demo default), 'github' and 'azure' (real). Capabilities: sourceControl (pull requests), issues (issues / work items).",
  "integrations": { "sourceControl": "fake", "issues": "fake" },

  "// github": "Target for the real 'github' provider — required only when integrations.sourceControl or .issues is 'github' (ignored otherwise). owner/repo name the repository, and that is all it carries: PR authorship and the assignee on items the harness FILES both follow userId now, so 'filters' and 'defaultAssignee' are retired keys — warned about at boot and dropped. All open issues are ingested (the watch gate decides what is acted on, not an ingest filter). Auth is NOT here — set the GITHUB_TOKEN env var so a secret never lands in a committed file.",
  "github": { "owner": "acme", "repo": "app" },

  "// azureDevOps": "Target for the real 'azure' provider (Azure DevOps Repos + Boards) — required only when integrations.sourceControl or .issues is 'azure' (ignored otherwise). organization/project/repository name the repo. The only filter left is filters.workItemTag, which narrows work items to one tag: identity-based narrowing follows userId, so filters.prAuthor, filters.workItemAssignedTo and defaultAssignee are retired keys — warned about at boot and dropped. Auth is NOT here — set AZURE_DEVOPS_PAT (Basic), or leave it unset to use the logged-in `az` CLI (Bearer).",
  "azureDevOps": {
    "organization": "acme",
    "project": "app",
    "repository": "app",
    "filters": { "workItemTag": "agent" },

    "// policyChecks": "Which Azure BRANCH-POLICY kinds become CI checks, and how. Kinds: build, status, comments, workItems, reviewers, mergeStrategy, other. Modes: 'check' (an ordinary CiCheck — visible, routable by a ci.checks rule, dispatchable), 'advisory' (visible and structurally unable to dispatch or escalate), 'off' (not emitted). Unset kinds take the defaults: build + status are 'check', comments is 'advisory' (rule pr-review-comment already models threads at higher fidelity), everything else 'off'. An unknown kind or mode throws at load. Nothing here can reach the aggregate ciStatus, so widening it can never make a PR read as unable to merge. The example promotes work-item linking so an agent fixes it — paired with the ci.checks guidance below.",
    "policyChecks": { "workItems": "check" }
  },

  "// labelPrefix": "The prefix behind the cockpit's watch toggle, shared by PRs and issues. It derives ONE label — '<prefix>-watch' ('work this') — and everything is opt-in: an item without the tag is left alone, pull requests and issues alike, and the harness tags the pull requests it opens itself so its own work never waits on a click. The old '<prefix>-ignore' tag is read nowhere any more. Set it to '' to turn the gate off entirely (every open item is worked), which is the first-run posture. Default 'lubbdubb'.",
  "labelPrefix": "lubbdubb",

  "// issuePriorityLabels": "Label -> priority weight for ordering issue pickup when headroom is limited (higher = first). REPLACED wholesale by an override, not merged, so you can define your own scheme (e.g. p0/p1/p2). Default { 'priority:high': 3, 'priority:medium': 2, 'priority:low': 1 }.",
  "issuePriorityLabels": { "priority:high": 3, "priority:medium": 2, "priority:low": 1 },

  "// issueDefaultPriority": "Weight for an issue carrying no matching priority label. Default 2.",
  "issueDefaultPriority": 2,

  "// issuePickupStates": "Dispatcher-level, state-based pickup gate. When non-empty, only issues whose provider-native workflow state is listed are picked up (e.g. ['Ready','Doing'] for Azure). Meaningful only for providers with a richer state model than open/closed (Azure work items); GitHub issues are unaffected. Unset/empty = no state gate.",
  "issuePickupStates": ["Ready", "Doing"],

  "// issueInReviewState": "The state a work item is moved to once a PR is open for it, so agents stop re-picking work already awaiting review/CI (e.g. 'In Review'). Takes effect only alongside issuePickupStates and needs a provider that can write state back (Azure). Unset = no automatic transition.",
  "issueInReviewState": "In Review",

  "// issueContainerTypes": "Provider-native item types that HOLD work rather than being work — an Azure DevOps Feature or Epic. One of these is never picked up, planned or assayed: its children are the work, and an agent put on the container would implement a decomposition that already exists beside it in the tracker. Meaningful only for providers that report an item type (Azure); GitHub issues carry none. Set [] to turn the gate off. Default ['Feature', 'Epic'], matched case-insensitively.",
  "issueContainerTypes": ["Feature", "Epic"],

  "// issueFilingTypes": "The work item types the harness may CREATE when you file a finding, a blueprint or unrecorded work. The filing agent picks one and is told it may create nothing else. The names are passed to the provider verbatim, so they must match your process template exactly — a Scrum project files ['Product Backlog Item', 'Bug']. Unlike issueContainerTypes there is no 'off': [] falls back to the default, because a work item is created AS something. Default ['User Story', 'Bug'].",
  "issueFilingTypes": ["User Story", "Bug"],

  "// ===== FEATURES =====": "The funnels and watches, and what each one costs you.",

  "// planning": "The planning funnel for multi-PR issues, and it is UNCONDITIONAL — every watched open issue first gets a PLANNING agent (rule issue-plan, on branch plan/issue/<n>) that reads the repo and submits a plan through the plan_submit tool (or .lubbdubb/plan.json as the fallback). A plan is one or more PARTS, each its own branch and pull request: rule plan-part puts one agent per part on issue/<n>/<slug>, stacked on its dependency's branch while that dependency's PR is open. Work that is one pull request is a plan with one part, not a shape of its own. EVERY plan is put to you before anything is scheduled from it: ingestion parks it as 'awaiting_approval', rule plan-approval proposes it once, and its ready parts queue as 'unapproved' in Up next until you accept — approve-before, because the undo for a plan that started itself is a replan, which is strictly worse than not starting. Rejecting retires the parts nothing was started for and sends the plan back to a planner with your reason. A planner that crashes or writes no plan fails OPEN after the usual attempt cap: the issue is worked whole on issue/<n> as it would be without plans, so a planner failure can never park an issue. Progress is mirrored to the tracker as ONE status comment per plan, edited in place; completion never closes the issue. There are no switches: 'planning.enabled' and 'planning.requireApproval' are retired keys, warned about at boot and dropped. Deep-merged.",
  "planning": {
    "// maxConcurrentPartsPerIssue": "How many parts of one plan may have agents at once — a cap rather than fanning the whole graph out as soon as dependencies are satisfied. These slots come out of maxConcurrentAgents, so one issue can occupy several of them. Default 2.",
    "maxConcurrentPartsPerIssue": 2,
    "// gitFetchIntervalMs": "Floor on how often plan reconciliation runs `git fetch` before reading branch reality. The git observer is fetch-free by design, so without a fetch it never sees a branch pushed from another machine — and 'has the dependency actually pushed' is what a stacked part waits on. This only stops a deliberately fast heartbeat becoming a fetch storm. 0 = every pulse. Default 60000.",
    "gitFetchIntervalMs": 60000
  },

  "// validation": "How anyone checks the GOAL was met, as steps a person or an agent runs rather than a paragraph nobody executes. Unconditional — a planner is asked for checks, a person marks them off, and the only consequence is that closing a goal with checks outstanding says so. The desktop channel is unconditional too: every start binds a stable socket your OWN Claude Code registers against, writes a 0600 credential and installs the /lubbdubb skill, so a check can run on a machine the fleet cannot reach. There is no switch for it — the paths below are the only choices, and they are how two harnesses share one machine. Deep-merged.",
  "validation": {
    "// desktopClaimMinutes": "How long a claimed check is held before it is offered again. Default 60.",
    "desktopClaimMinutes": 30
  },

  "// spendBurn": "The LIVE burn watch — the one cost reading taken while the money is still being spent. Every other spend surface is a post-mortem you have to go and open; this one compares a RUNNING agent's cost so far against the median of settled runs of its own kind of work, and files a visible 'burn' obligation when it is far past. The bucket is the dispatch rule AND the profile, never the rule alone: a goal pinned to 'deep' legitimately costs several times the same rule on 'fast', so a rule-only baseline would flag every pinned run and nothing else. It HOLDS NOTHING AND KILLS NOTHING — an expensive run is not a wrong run and no threshold can tell the two apart, so the notice is the prompt to open the transcript and decide. It refreshes its figures every pulse and settles itself when the run ends. Three gates must ALL hold, because a multiple on its own fires constantly and a notice you learn to dismiss unread is worse than none. On by default; deep-merged, so you can set one field alone. NOTE: PTY mode reports no usage at all, so on agentMode 'pty' nothing can ever trip this — that is the fail-open direction and the only safe one.",
  "spendBurn": {
    "// enabled": "Master switch. Default true. Off files nothing and STILL settles rows already standing, so turning it off drains the bench rather than stranding a row about a run that ended last Tuesday.",
    "enabled": true,
    "// multiple": "How many times its bucket's median a live run may reach before it surfaces. Must be above 1 (refused at load otherwise — at or below 1 it flags every run at or over the median). Default 4, generous on purpose: the spread inside one rule-and-profile bucket is real work, not noise.",
    "multiple": 4,
    "// minimumRuns": "Settled, measured runs a bucket needs before its median is trusted at all. Below it the bucket is SILENT rather than guessed at. Default 5.",
    "minimumRuns": 5,
    "// floorUsd": "Absolute money a run must ALSO have spent before a multiple counts as expensive. Four times the median of a rule that costs eight cents is thirty-two cents. Default 1.",
    "floorUsd": 1,
    "// ceilingUsd": "A flat per-run ceiling that fires with NO history whatever — the arm for a deployment where the first runaway is also the first run. Profile-blind and absolute, and the notice says so rather than claiming the run is unusual. Default null (no such arm): the right number is a property of your work and nothing here can guess it.",
    "ceilingUsd": null
  },

  "// selfUpdate": "Whether the harness checks its OWN build against upstream, and how often. Note what it does not name: a repo. The check runs against the directory LubbDubb is installed in, resolved from the running module, and never against repoRoot — a deployment working on someone else's codebase still wants to hear that its own build moved. On by default and cheap: the steady state is one ls-remote an hour, which transfers no objects. Deep-merged.",
  "selfUpdate": {
    "enabled": true,
    "// remote": "The remote the build is checked against. Configurable because a fork tracks somewhere else. Default 'origin'.",
    "remote": "origin",
    "// branch": "The branch the build is checked against. Default 'main'.",
    "branch": "main",
    "// checkIntervalMs": "How often the upstream tip is read. Default 3600000 (1h).",
    "checkIntervalMs": 3600000
  },

  "// ci": "Per-check CI policy: what the harness does about WHICH check, in WHICH state. Rules are ordered and matched by glob against the check name (* = any run of characters, ? = exactly one, case-insensitive) — and against any alias the provider reports for it, which is how an Azure status policy is matchable by the label shown on the PR page as well as by its statusGenre/statusName key. The first rule matching on BOTH name and state wins. REPLACED wholesale by an override, not merged. Empty (the default) — and any FAILING check matching no rule — is the pre-policy behaviour: dispatch a code agent with the generic fix prompt. So this is purely a way to carve exceptions. onFailure: 'dispatch' (fix it), 'ignore' (somebody else's wall — the DEFAULT for a rule you write, since the usual reason to name a check is to stop acting on it), 'escalate' (worth a human's eye). 'states' says which check states the rule claims, default ['failing']; the only other value is 'pending', for a BLOCKING check that never fails but sits queued forever until somebody runs something (rule pr-ci-gate dispatches for it, on its own origin pr:<n>:ci-gate with its own attempt cap, and a gate an agent cannot clear escalates rather than looping). 'states' SCOPES the rule rather than extending it — a rule listing only 'pending' no longer claims that check when it goes red, and the red one falls through to a later rule or to the unmatched dispatch. A check the provider reports as EXPIRED (an Azure build-validation policy whose last run predates the branch's commits, so nothing is in flight and nothing starts on its own) is watched with no rule naming it at all — so a pending-only rule with onFailure 'ignore' is how you mute that expiry chase, and because 'states' scopes rather than extends, the same check's genuine failures still dispatch. 'guidance' is appended to the agent's prompt and 'urgent' sorts this PR's concern ahead of every other PR concern this cycle — loadConfig THROWS if either is attached to a rule that never dispatches, if 'states' is empty or names anything but failing/pending, or if a rule that watches no failing state uses 'escalate' (there is no escalation arm for a merely waiting check, so it could never fire). The verdict is per PR, not per check: one agent works the branch, all matched guidance is appended to its one prompt, and held checks are NAMED to it so it doesn't chase them. When nothing is actionable but something escalates, rule pr-ci-blocked asks you once. Nothing here can move ciStatus or merge-readiness — a waiting gate never makes a PR read as failing.",
  "ci": {
    "checks": [
      { "match": "lint", "onFailure": "dispatch", "guidance": "Run the lint-fix skill; do not touch unrelated files." },
      {
        "match": "e2e (*)",
        "onFailure": "dispatch",
        "guidance": "These are flaky. Reproduce locally before editing test code."
      },
      { "match": "security-*", "onFailure": "dispatch", "urgent": true },
      { "match": "deploy-preview*", "onFailure": "ignore" },
      { "match": "infra-*", "onFailure": "escalate" },
      {
        "match": "pr-agent-review*",
        "states": ["pending"],
        "onFailure": "dispatch",
        "guidance": "Run `/pr-agent-review` on this branch. The gate clears when the review posts its status; nothing in the diff is broken."
      },
      { "match": "NXG-CI", "states": ["pending"], "onFailure": "ignore" },
      {
        "match": "Work item linking",
        "onFailure": "dispatch",
        "guidance": "Link the work item with `az repos pr work-item add --id <pr> --work-items <n>`. The work item number is the `<n>` in the branch name `issue/<n>`."
      }
    ]
  },

  "// COST NOTE": "planning, the assay, the assessor and the retrospective are all UNCONDITIONAL — there is no switch for any of them. One issue can therefore spend an assayer, a planner, its part agents, an assessor and a writer. Only an 'unclear' assay verdict holds anything, and it ends the moment the ticket is edited or anyone comments on it.",

  "// ===== PATHS =====": "Where the harness puts things. A relative path resolves against repoRoot, not the launch directory.",

  "// repoRoot": "The git repo the harness operates on; per-branch worktrees are cut from it. Defaults to the launch directory (process.cwd()), so the common case needs nothing. Set it (or the LUBBDUBB_REPO_ROOT env override) to point at a repo elsewhere; a relative path resolves against the launch dir.",
  "repoRoot": ".",

  "// defaultBranch": "The repository's integration branch. A NEW agent branch is cut from it (origin/<defaultBranch> preferred over the local ref), it is the base named in a PR's base-update prompt when the provider reports none, and a PR targeting anything else is treated as stacked. Existing branches/worktrees are reused untouched and never re-based. Not auto-detected — the harness may run against a clone whose HEAD is anywhere, and a wrong guess silently mis-bases work. Default 'main'.",
  "defaultBranch": "main",

  "// worktreeRoot": "Root under which the pool of worktree slot directories lives. Slots are LEASED to branches, not owned by them: a free slot already on your branch is handed back with its node_modules and build output intact, which is what stops every re-dispatch paying for a cold install, and a slot handed to a DIFFERENT branch is wiped first. How many there are is not a key — it is the live maxConcurrentAgents plus two, read on every dispatch, so raising the cap raises the pool with it and lowering the cap is how you say a disk cannot hold that many checkouts. ('worktreePoolSize' is a retired key, warned about at boot and dropped: a second bound could only sit above the cap, as disk nothing can lease, or below it, as the fleet's real limit with nothing saying so.) A relative value resolves against repoRoot (not the launch dir). Default '.lubbdubb/worktrees'.",
  "worktreeRoot": ".lubbdubb/worktrees",

  "// deskRoot": "Root under which desk (no-code) scratch dirs are created — one per task id. Relative resolves against repoRoot. Default '.lubbdubb/desk'.",
  "deskRoot": ".lubbdubb/desk",

  "// attachmentRoot": "Where images attached to a blueprint are written. The absolute path is load-bearing twice: it is what an agent's prompt names, and what the launch grants read access to. Relative resolves against repoRoot. Default '.lubbdubb/attachments'.",
  "attachmentRoot": ".lubbdubb/attachments",

  "// validationRoot": "Where validation resources are written, for the same two reasons as attachmentRoot. Relative resolves against repoRoot. Default '.lubbdubb/validation'.",
  "validationRoot": ".lubbdubb/validation",

  "// promptTemplatesDir": "Directory of operator overrides for the rule dispatcher's agent/escalation prompts. Each <prompt-id>.md replaces that prompt's built-in default; ids without a file keep theirs. A leading <!-- ... --> doc header is stripped before the prompt reaches the agent. A relative path resolves against repoRoot. Absent directory = all built-in defaults. Default '.lubbdubb/prompts'.",
  "promptTemplatesDir": ".lubbdubb/prompts",

  "// docsFolderPrefix": "Folder(s) the file-events hook treats as the artifacts area: any file an agent writes UNDER a prefix is promoted to an artifact chip regardless of extension, on top of the built-in report/doc heuristic (report extensions + any reports/ folder). Accepts a string or an array; a file promotes under ANY entry. A relative entry is worktree-relative; an ABSOLUTE entry (e.g. D:/docs) also matches files written under that real directory OUTSIDE the worktree and widens the artifact-serving boundary to include it. Unset = heuristic only.",
  "docsFolderPrefix": ["docs", "D:/shared/reports"],

  "// dbPath": "SQLite file. Overridable via the LUBBDUBB_DB env var. ':memory:' is supported (tests). Default '.lubbdubb/lubbdubb.sqlite'.",
  "dbPath": ".lubbdubb/lubbdubb.sqlite",

  "// ===== SERVER =====": "Where the cockpit listens, and who may reach it.",

  "// port": "HTTP/WS port for the server + cockpit. Overridable via the PORT env var. Default 4300.",
  "port": 4300,

  "// host": "Bind address. Default '127.0.0.1' — this machine only, because the cockpit can queue jobs and a job spawns an agent with write access to your repo. Set '0.0.0.0' to expose it on your network; doing that with auth.enabled false is REFUSED at startup. Overridable via LUBBDUBB_HOST.",
  "host": "127.0.0.1",

  "// auth": "Cockpit access control. 'enabled' (default true) requires a bearer token on every /api/* route and on /ws. There is deliberately no token key here — a secret must not live in a committable file: set LUBBDUBB_TOKEN, or let the harness mint one into 'tokenFile' (0600) and print it as the #t= link at startup. Deep-merged, so setting tokenFile alone does not silently disable it.",
  "auth": { "enabled": true, "tokenFile": ".lubbdubb/cockpit-token" }
}
