<!--
  Sent to a code agent when an open work item / issue has no linked PR and no agent is on it (rule 4). Placeholders: {number} {title} {body} {branch}.
-->

GitHub issue #{number} ("{title}") needs resolving.

{body}

Implement the fix on branch {branch} and open a pull request that closes this issue.
